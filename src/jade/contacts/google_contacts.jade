ons-page(ng-controller="googleContactsCtrl")
  ons-toolbar
    .center Google Kontakte
    .right
      ons-toolbar-button(ng-click="refreshContacts()")
        ons-icon(icon="refresh")
  section(style="padding: 8px")
    ons-list
      ons-list-item(ng-repeat="contact in contacts", ng-class="{ selected: contact.selected }", ng-click="toggleContact(contact)") {{contact.displayName}}

  section(align="center")
    ons-button(ng-click="contactsNav.popPage()") Hinzufügen


