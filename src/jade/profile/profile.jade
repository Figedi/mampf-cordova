ons-navigator(ng-controller="ProfileCtrl")
  ons-page
    ons-toolbar
      .center Profil
    section(style="padding: 8px")
      label.form-label Handynummer
      br
      input(class="text-input", ng-model="user.telephone", placeholder="Bitte ihre eigene Handynummer eingeben", style="display: block; width: 100%")
    .profileButtons
      ons-row(style="padding: 8px")
        ons-col(align="center")
          ons-button(ng-click="ons.navigator.pushPage('partials/profile/showLocationsPage.html', { animation : 'slide' } )")
            ons-icon(icon="map-marker")
            | Orte anzeigen

      ons-row(style="padding: 8px")
        ons-col(align="center")
          ons-button(ng-click="ons.navigator.pushPage('partials/profile/addNewLocationPage.html', { animation : 'slide' } )")
            ons-icon(icon="plus")
            | Ort hinzufügen
