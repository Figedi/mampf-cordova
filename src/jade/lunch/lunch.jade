ons-navigator
  ons-page
    ons-toolbar
      .center Neuer Essensvorschlag
    section(style="margin-top: 20px; padding: 8px" )
      | Start- und Endzeit festlegen
      ons-row
        ons-col
          input(type="time", class="text-input", ng-model="startTime", placeholder="12:00", style="display: block; width: 95%")
        ons-col
          input(type="time", class="text-input", ng-model="endTime", placeholder="14:00", style="display: block; width: 95%")

    section(style="padding: 8px")
      ons-button(ng-click="ons.navigator.pushPage('partials/lunch/chooseLocationPage.html')" )
        | Ort auswählen
        ons-icon(icon="arrow-right" )
      br
      | Ausgewählter Ort: test

    section(style="padding: 8px")
      ons-button(ng-click="ons.navigator.pushPage('partials/lunch/chooseContactsPage.html')" )
        | Kontakte auswählen
        ons-icon(icon="arrow-right" )
      br
      | Anzahl Kontakte: testNummer

    ons-row(style="padding: 16px;")
      ons-col(align="center")
        ons-button(ng-click="ons.navigator.pushPage('partials/lunch/showResultsFailurePage.html')" )
          | Anfrage verschicken
          ons-icon(icon="cutlery")
